# Copy to .env and fill in. Get a key at https://console.groq.com/keys
# Without a key the app runs in deterministic MOCK mode, so the whole pipeline
# still works offline (no LLM calls, no cost).
GROQ_API_KEY=

# Force mock mode even if a key is present (useful for CI / demos).
FORCE_MOCK=false
